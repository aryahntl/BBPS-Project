import { Component } from '@angular/core';

@Component({
  selector: 'app-root', // Ensure the selector is app-root
  template: '<app-subject-management></app-subject-management>', // Ensure the template is valid
  styleUrls: ['./app.component.css'],
})
export class AppComponent {}
